spring:
  application:
    name: aski
  datasource:
    url: jdbc:postgresql://localhost:5432/aski
    username: postgres
    password: ***
  jpa:
    hibernate:
      ddl-auto=update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 366526718418-60to21lqsq6m7plarju8jugc3h2k1lcr.apps.googleusercontent.com
            client-secret: ***
              
server:
  port: 5000
